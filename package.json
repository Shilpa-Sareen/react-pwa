{
  "name": "pwa-serve",
  "version": "1.0.0",
  "description": "test pwa server",
  "main": "server.js",
  "scripts": {
    "test": "test",
    "start": "cd UI && npm i && npm run build && cd .. && npm i express.js && node server.js"
  },
  "author": "Shilpa",
  "license": "ISC",
  "dependencies": {
    "express.js": "^1.0.0"
  }
}
